.container
  width 100%
  max-width 800px
  margin 0 auto

.mono-space {
  font-family: "Courier New", Courier, monospace
}

* {
  box-sizing: border-box;
}

.clearfix {
  clear both
}

.cut-text {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.qr img {
  width 100%
}
